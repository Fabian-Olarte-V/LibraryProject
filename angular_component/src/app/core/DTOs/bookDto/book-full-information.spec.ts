import { BookFullInformation } from './book-full-information';

describe('BookFullInformation', () => {
  it('should create an instance', () => {
    expect(new BookFullInformation()).toBeTruthy();
  });
});
